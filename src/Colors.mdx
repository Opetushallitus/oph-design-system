import { ophColors } from './colors';
import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

<Meta title="Colors" />

<ColorPalette>
  <ColorItem
    title="Harmaa"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('grey'),
      ),
    )}
  />
  <ColorItem
    title="Neutraali"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.match(/black|white/),
      ),
    )}
  />
  <ColorItem
    title="Sininen"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.match(/blue|cyan/i),
      ),
    )}
  />
  <ColorItem
    title="Vihreä"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('green'),
      ),
    )}
  />
  <ColorItem
    title="Punainen"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('red'),
      ),
    )}
  />
  <ColorItem
    title="Oranssi"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('orange'),
      ),
    )}
  />
  <ColorItem
    title="Keltainen"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('yellow'),
      ),
    )}
  />
  <ColorItem
    title="Pinkki"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('pink'),
      ),
    )}
  />
  <ColorItem
    title="Purppura"
    colors={Object.fromEntries(
      Object.entries(ophColors).filter(([name, color]) =>
        name.startsWith('purple'),
      ),
    )}
  />
  <ColorItem
    title="Semanttinen"
    colors={{
      success: ophColors.alias.success,
      error: ophColors.alias.success,
    }}
  />
</ColorPalette>
